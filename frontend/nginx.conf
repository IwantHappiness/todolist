server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location /tasks {
        proxy_pass http://host.docker.internal:8080/tasks;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
